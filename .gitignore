# =============================================================================
# AI CATALOGUE PROJECT - GITIGNORE
# =============================================================================

# Environment Configuration (CRITICAL - Contains Secrets)
.env
.env.local
.env.*.local
*.env

# Azure Deployment Scripts (Contains Credentials/Infrastructure Config)
k8s/scripts/azure-setup.sh
k8s/scripts/azure-*.sh

# =============================================================================
# SYSTEM & IDE FILES
# =============================================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE/Editor Files
.vscode/
.idea/
*.swp
*.swo
*~

# =============================================================================
# PYTHON/DJANGO BACKEND
# =============================================================================
# Python Virtual Environment
backend/venv/
backend/env/
backend/.venv/
venv/
env/
.venv/

# Python Cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# Python build directories (not frontend src/lib)
backend/lib/
lib64/
**/lib/
!frontend/**/src/lib/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Django
backend/db.sqlite3
backend/db.sqlite3-journal
backend/staticfiles/
backend/media/uploads/
backend/logs/

# Celery
celerybeat-schedule
celerybeat.pid

# =============================================================================
# NODE.JS/SVELTE FRONTEND
# =============================================================================
# Dependencies
frontend/node_modules/
frontend/npm-debug.log*
frontend/yarn-debug.log*
frontend/yarn-error.log*
frontend/.pnpm-debug.log*

# Build Output
frontend/build/
frontend/dist/
frontend/.svelte-kit/
frontend/.next/
frontend/out/

# Runtime
frontend/.npm
frontend/.eslintcache

# =============================================================================
# DOCKER & CONTAINERIZATION
# =============================================================================
# Ignore built images info
.docker/
*.tar

# =============================================================================
# KUBERNETES & CLOUD DEPLOYMENT
# =============================================================================
# Kubernetes Secrets (Generated)
k8s/secrets/
k8s/*/secrets.yaml
k8s/*/secret-*.yaml

# Kubernetes Config (Generated)
kubeconfig
*.kubeconfig
.kube/

# Azure/Cloud Files
.azure/
*.publishsettings
*.azurePubxml

# Terraform (if used)
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
.terraform/

# =============================================================================
# DATABASE & STORAGE
# =============================================================================
# Database Files
*.db
*.sqlite
*.sqlite3

# PostgreSQL
*.sql
*.dump

# Milvus/Vector Database
volumes/milvus/
volumes/postgres/
volumes/minio/

# Logs Directory
logs/
*.log
*.out

# =============================================================================
# BACKUPS & TEMPORARY FILES
# =============================================================================
# Backups
backup/
*.bak
*.backup
*.orig

# Temporary Files
tmp/
temp/
.tmp/
.temp/

# OS Generated Files
*.tmp
*.temp
*~
.fuse_hidden*

# =============================================================================
# AI/ML SPECIFIC
# =============================================================================
# Model Files (Usually Large)
*.h5
*.pkl
*.pickle
*.pt
*.pth
*.ckpt
*.safetensors

# Jupyter Notebooks Checkpoints
.ipynb_checkpoints/

# =============================================================================
# API KEYS & SECRETS
# =============================================================================
# API Keys Files
api_keys.txt
secrets.txt
credentials.json
service-account*.json
*.pem
*.key
*.crt
*.p12
*.pfx

# =============================================================================
# MONITORING & OBSERVABILITY
# =============================================================================
# Grafana
grafana/data/
prometheus/data/

# =============================================================================
# DEVELOPMENT & TESTING
# =============================================================================
# Test Coverage
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# =============================================================================
# PROJECT SPECIFIC EXCLUSIONS
# =============================================================================
# Claude AI Files (Keep for development, exclude sensitive parts)
.claude/conversations/
.claude/cache/

# Trash/Archive Directories
trash/
archive/
deleted/

# Large Data Files
*.csv
# Ignore large JSON data files but not package.json files
*.json
!package.json
!package-lock.json
!**/package.json
!**/package-lock.json
# Reference material, not data: the workflow JSON Schema and a synthetic
# example bundle a cloner can import to see the engine run. Both are
# deliberately free of real prompts, URLs and project content.
!backend/schemas/agent_workflow_schema.json
!backend/schemas/example_workflow.json
data/
datasets/

# Generated Documentation
docs/_build/
documentation/

# LaTeX/Academic Papers
*.tex
*.bib

# =============================================================================
# GitHub Workflows
.github/workflows/sync-upstream.yml

# Claude Code config (local-only, not needed by other contributors)
CLAUDE.md

# E2E test script (contains hardcoded local paths and project UUIDs)
test_workflow_e2e.py

# NON-PROJECT / RESEARCH ARCHIVE
# =============================================================================
# All non-project files (old paper drafts, figures, research, docs) live here
_archive/

# Research evaluation scripts inside backend (not part of the product)
backend/_race_eval/


# =============================================================================
# KUBERNETES FOLDER
# =============================================================================
# Kubernetes deployment configurations (infrastructure - not needed for CORS implementation)
k8s/

# =============================================================================
# SHELL SCRIPTS - ALLOWED
# =============================================================================
# All shell scripts are now safe to include as they only reference environment variables
# and don't contain hardcoded credentials

# NOTE: scripts/ and nginx/ folders are now tracked in git
# so you can work on them from multiple remote repositories
# (k8s/ remains ignored as it's infrastructure config, not part of the implementation)

# SSL/TLS Certificates and Keys
certbot/
certbot/certs/
certbot/certs/live/
certbot/certs/archive/
certbot/certs/accounts/
ssl/
*.crt
*.key
*.pem
*.p12
*.pfx
*.csr

# Backup Files
backup/
*.backup
*.bak
*.dump

# Local Environment Overrides
docker-compose.local.yml
docker-compose.override.local.yml

# ============================================================================
# SECRET SAFETY NET — this repository is PUBLIC.
# Anything matching these must never be committed. Added 2026-08-06.
# The scripts/git-hooks/pre-commit hook enforces this at commit time; install
# it once per clone with ./scripts/install-git-hooks.sh
# ============================================================================

# SSH / PGP private keys
id_rsa
id_rsa.*
id_dsa
id_ecdsa
id_ed25519
*.ppk
*.gpg
*.asc
.ssh/

# Runtime data directories (may contain database files / uploads)
volumes/
/logs/
media/

# Cloud provider credentials
.aws/
.azure/
gcloud-service-key.json
*-service-account*.json
azure-credentials*.json
kubeconfig
*.kubeconfig

# Anything explicitly named as a secret
*secret*.yml
*secret*.yaml
*secrets*.json
*.secrets
.secrets/
*password*.txt
*credentials*.txt

# Database dumps / archives that may embed data
*.dump
*.bak
